# Burn fAIke

The AI-Powered Conspiracy & Viral Rumor Trace Engine.

## Local

Keys go in `.env` (already gitignored). Example names: `.env.example`.

```bash
npm install
npm run dev
```

Open http://localhost:3000
